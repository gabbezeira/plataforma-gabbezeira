import { Container } from './styles'

export function Alert() {
	return (
		<Container>
			<div className="alert">
				Atenção! Não hospedamos nenhum arquivo no site, apenas os indexamos, por
				isso é completamente dentro da lei!
			</div>
		</Container>
	)
}
